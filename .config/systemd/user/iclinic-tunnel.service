[Unit]
Description=SSH tunnels to iclinic (SOCKS + port forwards)
After=network-online.target
Wants=network-online.target

[Service]
Environment=AUTOSSH_GATETIME=0
ExecStart=/usr/bin/autossh -M 0 -N iclinic-tunnel
Restart=always
RestartSec=30

[Install]
WantedBy=default.target
